[tox]
envlist = py36

[travis]
python =
    3.6: py36

[testenv]
passenv = CODACY_PROJECT_TOKEN
setenv =
    TEST_NET=ganache
    PYTHONPATH = {toxinidir}
    CONFIG_FILE = {toxinidir}/config_local.ini
    PARITY_ADDRESS1=0x00bd138abd70e2f00903268f3db08f2d25677c9e
    PARITY_PASSWORD1=node0
    PARITY_KEYFILE1={toxinidir}/tests/resources/keys/key_file_2.json
    PARITY_ADDRESS=0x068ed00cf0441e4829d9784fcbe7b9e26d4bd8d0
    PARITY_PASSWORD=secret
    PARITY_KEYFILE={toxinidir}/tests/resources/keys/key_file_1.json
    TEST_PRIVATE_KEY1=0xbbfbee4961061d506ffbb11dfea64eba16355cbf1d9c29613126ba7fec0aed5d
    TEST_PRIVATE_KEY2=0x804365e293b9fab9bd11bddd39082396d56d30779efbb3ffb0a6089027902c4a

deps =
    -r{toxinidir}/requirements_dev.txt
; If you want to make tox run the tests with the same versions, create a
; requirements.txt with the pinned versions and uncomment the following line:
;     -r{toxinidir}/requirements.txt
commands =
    pip install -U pip
    pip list
    python deploy2.py ganache artifacts/addresses.json
    coverage run --source ocean_lib -m --omit ocean_lib/examples/* py.test  --basetemp={envtmpdir}
    coverage report
    coverage xml
